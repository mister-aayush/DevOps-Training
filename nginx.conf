server {
    listen 80;
    server_name webapp.local;

    root /var/www/webapp/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    error_page 404 /404.html;

    access_log /var/log/nginx/webapp_access.log;
    error_log  /var/log/nginx/webapp_error.log;
}

